apiVersion: v1
kind: Service
metadata:
  name: test-service
  namespace: resource-injection-test
  labels:
    app: test-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: test-app